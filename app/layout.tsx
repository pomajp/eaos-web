import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EAOS - Executive Administrator Operating System",
  description: "Turn every text, email, and voice memo into a living COO for the CEO–EA dyad. Everything you need on one page, every morning.",
  openGraph: {
    title: "EAOS - Executive Administrator Operating System",
    description: "Turn every text, email, and voice memo into a living COO for the CEO–EA dyad.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}